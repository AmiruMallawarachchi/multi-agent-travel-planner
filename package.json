{
  "name": "tripweaver",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend run build",
    "vercel-build": "npm --prefix frontend run build"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "next": "15.5.20"
  }
}
