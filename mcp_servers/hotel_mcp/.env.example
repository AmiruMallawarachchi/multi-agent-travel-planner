# Copy to .env for local development. In Render, set this as a service variable.
# The same SerpApi key can be reused by flight-mcp.
SERPAPI_API_KEY=your-serpapi-api-key
SERPAPI_BASE_URL=https://serpapi.com/search.json

# Local development default; Render sets PORT automatically.
PORT=8001
